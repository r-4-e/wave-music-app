<?xml version='1.0' encoding='utf-8'?>
<widget id="com.wave.musicapp" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:cdv="http://cordova.apache.org/ns/1.0">

    <name>Wave</name>
    <description>A personal music streaming app with a Spotify-style interface and YouTube-based audio extraction.</description>
    <author email="you@example.com" href="https://example.com">Your Name</author>

    <!-- Entry point -->
    <content src="index.html" />

    <!-- ============================================================
         NETWORK ACCESS
         Required so the app can reach the Cobalt API, Invidious
         instances, lyric APIs, and YouTube thumbnail/audio hosts.
         ============================================================ -->
    <access origin="*" />
    <allow-navigation href="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />

    <!-- ============================================================
         GENERAL PREFERENCES
         ============================================================ -->
    <preference name="DisallowOverscroll" value="true" />
    <preference name="BackgroundColor" value="0xFF121212" />
    <preference name="Orientation" value="portrait" />
    <preference name="Fullscreen" value="false" />
    <preference name="AndroidXEnabled" value="true" />
    <preference name="StatusBarOverlaysWebView" value="false" />
    <preference name="StatusBarBackgroundColor" value="#121212" />
    <preference name="StatusBarStyle" value="lightcontent" />
    <preference name="SplashMaintainAspectRatio" value="true" />
    <preference name="AndroidPersistentFileLocation" value="Internal" />
    <preference name="loadUrlTimeoutValue" value="60000" />

    <!-- ============================================================
         ANDROID PLATFORM (target: Android 12 / API 31-32)
         ============================================================ -->
    <platform name="android">
        <preference name="android-minSdkVersion" value="24" />
        <preference name="android-targetSdkVersion" value="32" />
        <preference name="android-compileSdkVersion" value="33" />
        <preference name="AndroidLaunchMode" value="singleTask" />
        <preference name="AndroidWindowSplashScreenAnimatedIcon" value="false" />

        <!--
          Foreground-service + media + notification permissions needed for:
            - cordova-plugin-background-mode (keep JS/audio alive when
              the screen is locked or the app is minimized on Android 12)
            - cordova-plugin-music-controls (lock-screen / notification
              media controls, including the Android 13 notification
              permission requirement)
        -->
        <edit-config file="AndroidManifest.xml" mode="merge" target="/manifest" xmlns:android="http://schemas.android.com/apk/res/android">
            <uses-permission android:name="android.permission.INTERNET" />
            <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
            <uses-permission android:name="android.permission.WAKE_LOCK" />
            <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
            <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
            <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
            <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
        </edit-config>
    </platform>

    <!-- ============================================================
         PLUGINS
         ============================================================ -->

    <!-- Enables the <access>/<allow-navigation> whitelist rules above -->
    <plugin name="cordova-plugin-whitelist" spec="^1.3.5" />

    <!-- Device info (used internally by several plugins) -->
    <plugin name="cordova-plugin-device" spec="^2.1.0" />

    <!--
      Keeps the WebView's JS execution (and therefore audio playback)
      running when the screen is locked or the app is minimized.
      Required for Android 12 to avoid the OS killing/throttling the
      webview process during background playback.
    -->
    <plugin name="cordova-plugin-background-mode" spec="^0.7.3" />

    <!--
      System media controls: broadcasts track metadata (title, artist,
      cover art) to the Android notification drawer and lock screen,
      and routes play/pause/next/previous back into index.js via
      MusicControls.subscribe().

      cordova-plugin-music-controls2 is the actively-maintained fork of
      the original cordova-plugin-music-controls and exposes the same
      global `MusicControls` JS object used in www/js/index.js.
    -->
    <plugin name="cordova-plugin-music-controls2" spec="^3.0.6" />

</widget>
